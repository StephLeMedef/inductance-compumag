[tool.black]
    metadata_filter = '{"tags": ["keep_outputs"]}'
    include = '\.pyi?$|\.ipynb$'
    exclude = '''
    /(
        \.git
      | \.hg
      | \.mypy_cache
      | \.tox
      | \.venv
      | _build
      | buck-out
      | build
      | dist
    )/
'''